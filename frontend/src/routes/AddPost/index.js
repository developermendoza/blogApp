import React from "react";
import { Typography, Box, Toolbar } from "@mui/material";

const AddPost = () => {
  return (
    <div>
      <Box component="main" sx={{ p: 3 }}>
        <Toolbar />
        <Typography>AddPost</Typography>
      </Box>
    </div>
  );
};

export default AddPost;
