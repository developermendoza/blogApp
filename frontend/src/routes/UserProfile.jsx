import React from "react";
import { Box, Container, Toolbar } from "@mui/material";

const UserProfile = () => {
  return (
    <Box as="main">
      <Container>
        <Toolbar />
        <p>UserProfile</p>
      </Container>
    </Box>
  );
};

export default UserProfile;
