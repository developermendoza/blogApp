import React from "react";
import { Box, Container, Toolbar } from "@mui/material";

const Logout = () => {
  return (
    <Box as="main">
      <Container>
        <Toolbar />
        <p>Logout</p>
      </Container>
    </Box>
  );
};

export default Logout;
